# Webhook authentication secret — must match the value configured in n8n
# Set this in Cloudflare Pages → Settings → Environment Variables
WEBHOOK_SECRET=your-secret-here

# PIN to access the /dashboard page (digits recommended, e.g. 6-digit code)
# Set this in Cloudflare Pages → Settings → Environment Variables
DASHBOARD_PIN=123456
